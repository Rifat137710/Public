# Paper — ISGT submission

`isgt2026.tex` · IEEEtran `conference` class. No LaTeX toolchain in this
repository; build on Overleaf or any TeX Live install:

```
latexmk -pdf isgt2026.tex
```

## The rule that matters

**No number is typed into the `.tex` by hand.** Every table is `\input` from
`tables/`, generated by:

```
python scripts/make_paper_tables.py
```

which reads `results/*.json` directly. Regenerate after any re-run. A
transcription error in a table is invisible to every check this repository
runs — the tests, the reproduction gate and the sweeps all pass regardless of
what ends up in the manuscript, so the manuscript must not be a separate copy
of the data.

| table | source | claim |
|---|---|---|
| `testbeds.tex` | `kerber_operating_point.json` | the two feeders share nothing |
| `layer_not_learner.tex` | `projected_heuristics.json`, `ablation/ablation.json` | C5 — 8.0× |
| `staleness.tex` | `staleness_sweep_25ep.json`, `staleness_kerber.json` | C1, C2 — the headline |
| `model_error.tex` | `model_error_case33bw.json`, `model_error_kerber.json` | C3 — 0.80×–2.06× |
| `diagnostic.tex` | `staleness_kerber.json`, `model_error_case33bw.json` | C4 — the spine |

## Claims are frozen

`docs/00-state-of-play.md` §2b. Do not strengthen them while drafting. Each is
scoped to exactly what was measured, and that scoping is what keeps the journal
work from contradicting a published sentence — see `docs/08-retroactive-risk.md`
for which claim each edit protects.

Reporting rules that travel with the claim set:

- three request sources on the MV feeder, **two** on the LV feeder — never
  "three sources" flat
- point estimates only; no confidence intervals claimed
- weak, low-SCR feeders only; no generalisation to well-built distribution
- refresh is simulated by recomputing power flow, not by a metering pipeline
- the cliff **location** is not a universal number — never "refresh within 2 h"

## Before submission

| # | Item | State |
|---|---|---|
| 1 | Read Bai *et al.* (TPWRS 2022) in full — nearest prior art | ⬜ **do first** |
| 2 | Verify every bibliography entry against the paper itself | ⬜ |
| 3 | Co-authors, affiliations, acknowledgment | ⬜ user's call |
| 4 | Figures — see below | ⬜ |
| 5 | Page-limit pass once the venue is fixed | ⬜ |

## Figures still to draw

1. **Violation rate vs base-point age**, both feeders, log-x — the cliff. This
   is the paper's one essential figure.
2. **Single-episode trace** — voltage at the critical bus plus station
   set-points, fresh vs stale. PES reviewers respond to this more than to any
   aggregate.
3. *(optional)* safety–service scatter with the uncoordinated↔droop mixture
   line, showing the projected arms dominating it.
